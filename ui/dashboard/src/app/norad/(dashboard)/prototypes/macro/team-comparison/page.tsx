// src/app/(dashboard)/prototypes/macro/team-comparison/page.tsx
// Simplified placeholder to avoid build-time runtime errors.

export const revalidate = 300

export default function TeamComparisonPage() {
  return (
    <div className="space-y-6">
      <div className="bg-card rounded-xl border border-border p-6">
        <h1 className="font-display text-2xl font-bold mb-2">Team Comparison</h1>
        <p className="text-sm text-muted-foreground">
          This prototype page has been temporarily simplified for production deployment.
          The full interactive version will be re-enabled after the unified site is successfully deployed.
        </p>
      </div>
    </div>
  )
}
