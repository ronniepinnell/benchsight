-- Fact Tables
-- Load after dimension tables

-- fact_shifts: 63 columns
CREATE TABLE IF NOT EXISTS fact_shifts (
    shift_key VARCHAR(50) PRIMARY KEY,
    game_id INTEGER,
    shift_index INTEGER,
    Period INTEGER,
    shift_start_min INTEGER,
    shift_start_sec INTEGER,
    shift_end_min INTEGER,
    shift_end_sec INTEGER,
    shift_start_type VARCHAR(100),
    shift_stop_type VARCHAR(100),
    home_forward_1 INTEGER,
    home_forward_2 INTEGER,
    home_forward_3 INTEGER,
    home_defense_1 INTEGER,
    home_defense_2 INTEGER,
    home_xtra INTEGER,
    home_goalie INTEGER,
    away_forward_1 INTEGER,
    away_forward_2 INTEGER,
    away_forward_3 INTEGER,
    away_defense_1 INTEGER,
    away_defense_2 INTEGER,
    away_xtra TEXT,
    away_goalie INTEGER,
    stoppage_time INTEGER,
    home_ozone_start INTEGER,
    home_ozone_end INTEGER,
    home_dzone_start INTEGER,
    home_dzone_end INTEGER,
    home_nzone_start INTEGER,
    home_nzone_end INTEGER,
    home_team VARCHAR(20),
    away_team VARCHAR(20),
    shift_start_total_seconds INTEGER,
    shift_end_total_seconds INTEGER,
    shift_duration INTEGER,
    home_team_strength INTEGER,
    away_team_strength INTEGER,
    home_team_en BOOLEAN,
    away_team_en BOOLEAN,
    home_team_pk INTEGER,
    home_team_pp INTEGER,
    away_team_pp INTEGER,
    away_team_pk INTEGER,
    situation VARCHAR(100),
    strength VARCHAR(100),
    home_goals INTEGER,
    away_goals INTEGER,
    home_team_plus INTEGER,
    home_team_minus INTEGER,
    away_team_plus INTEGER,
    away_team_minus INTEGER,
    period_start_total_running_seconds INTEGER,
    running_video_time INTEGER,
    shift_start_running_time INTEGER,
    shift_end_running_time INTEGER,
    strength_id INTEGER,
    situation_id INTEGER,
    shift_start_type_id INTEGER,
    shift_stop_type_id INTEGER,
    home_team_id VARCHAR(20),
    away_team_id VARCHAR(20),
    period_id INTEGER
);

-- fact_events: 54 columns
CREATE TABLE IF NOT EXISTS fact_events (
    event_key VARCHAR(50) PRIMARY KEY,
    game_id INTEGER,
    period INTEGER,
    event_index INTEGER,
    linked_event_index INTEGER,
    sequence_index INTEGER,
    play_index INTEGER,
    event_type VARCHAR(100),
    tracking_event_index INTEGER,
    event_start_min INTEGER,
    event_start_sec INTEGER,
    event_end_min INTEGER,
    event_end_sec INTEGER,
    role_abrev VARCHAR(20),
    event_team_zone VARCHAR(20),
    home_team_zone VARCHAR(20),
    away_team_zone VARCHAR(20),
    team_venue VARCHAR(20),
    team_venue_abv VARCHAR(20),
    side_of_puck VARCHAR(20),
    play_detail1 VARCHAR(100),
    play_detail_2 VARCHAR(100),
    play_detail_successful VARCHAR(20),
    pressured_pressurer INTEGER,
    zone_change_index INTEGER,
    home_team VARCHAR(20),
    away_team VARCHAR(20),
    Type VARCHAR(100),
    event_detail VARCHAR(100),
    event_detail_2 VARCHAR(100),
    event_successful VARCHAR(20),
    shift_index INTEGER,
    duration INTEGER,
    time_start_total_seconds INTEGER,
    time_end_total_seconds INTEGER,
    running_intermission_duration INTEGER,
    period_start_total_running_seconds INTEGER,
    running_video_time INTEGER,
    event_running_start INTEGER,
    event_running_end INTEGER,
    period_id INTEGER,
    venue_id INTEGER,
    event_type_id INTEGER,
    event_detail_id INTEGER,
    event_detail_2_id INTEGER,
    success_id INTEGER,
    play_detail_success_id INTEGER,
    zone_id INTEGER,
    play_detail_id INTEGER,
    play_detail_2_id INTEGER,
    home_team_id VARCHAR(20),
    away_team_id VARCHAR(20),
    shift_key VARCHAR(50),
    empty_net_goal BOOLEAN
);

-- fact_events_player: 63 columns
CREATE TABLE IF NOT EXISTS fact_events_player (
    event_player_key VARCHAR(50) PRIMARY KEY,
    event_key VARCHAR(50),
    game_id INTEGER,
    player_id VARCHAR(20),
    event_index INTEGER,
    player_role VARCHAR(100),
    role_number INTEGER,
    period INTEGER,
    event_type VARCHAR(100),
    player_game_number INTEGER,
    linked_event_index INTEGER,
    tracking_event_index INTEGER,
    event_start_min INTEGER,
    event_start_sec INTEGER,
    event_end_min INTEGER,
    event_end_sec INTEGER,
    role_abrev VARCHAR(20),
    event_team_zone VARCHAR(20),
    home_team_zone VARCHAR(20),
    away_team_zone VARCHAR(20),
    team_venue VARCHAR(20),
    team_venue_abv VARCHAR(20),
    side_of_puck VARCHAR(20),
    sequence_index INTEGER,
    play_index INTEGER,
    play_detail1 VARCHAR(100),
    play_detail_2 VARCHAR(100),
    play_detail_successful VARCHAR(20),
    pressured_pressurer INTEGER,
    zone_change_index INTEGER,
    home_team VARCHAR(20),
    away_team VARCHAR(20),
    Type VARCHAR(100),
    event_detail VARCHAR(100),
    event_detail_2 VARCHAR(100),
    event_successful VARCHAR(20),
    shift_index INTEGER,
    duration INTEGER,
    time_start_total_seconds INTEGER,
    time_end_total_seconds INTEGER,
    running_intermission_duration INTEGER,
    period_start_total_running_seconds INTEGER,
    running_video_time INTEGER,
    event_running_start INTEGER,
    event_running_end INTEGER,
    player_team VARCHAR(20),
    player_name VARCHAR(200),
    play_detail VARCHAR(100),
    period_id INTEGER,
    venue_id INTEGER,
    event_type_id INTEGER,
    event_detail_id INTEGER,
    event_detail_2_id INTEGER,
    success_id INTEGER,
    play_detail_success_id INTEGER,
    zone_id INTEGER,
    play_detail_id INTEGER,
    play_detail_2_id INTEGER,
    role_id INTEGER,
    home_team_id VARCHAR(20),
    away_team_id VARCHAR(20),
    player_team_id VARCHAR(20),
    shift_key VARCHAR(50)
);

-- fact_shifts_player: 35 columns
CREATE TABLE IF NOT EXISTS fact_shifts_player (
    shift_player_key VARCHAR(50) PRIMARY KEY,
    shift_key VARCHAR(50),
    game_id INTEGER,
    shift_index INTEGER,
    player_number INTEGER,
    venue VARCHAR(20),
    slot VARCHAR(20),
    period INTEGER,
    shift_duration INTEGER,
    situation VARCHAR(100),
    strength VARCHAR(20),
    stoppage_time INTEGER,
    pm_plus_ev INTEGER,
    pm_minus_ev DECIMAL(15,6),
    goal_for INTEGER,
    goal_against INTEGER,
    team_en BOOLEAN,
    opp_en BOOLEAN,
    team_pp INTEGER,
    team_pk INTEGER,
    player_id VARCHAR(20),
    player_name VARCHAR(200),
    logical_shift_number INTEGER,
    shift_segment INTEGER,
    cumulative_shift_duration INTEGER,
    running_toi INTEGER,
    playing_duration INTEGER,
    running_playing_toi INTEGER,
    period_id INTEGER,
    venue_id INTEGER,
    strength_id INTEGER,
    situation_id INTEGER,
    slot_id INTEGER,
    team_id VARCHAR(20),
    team_name VARCHAR(200)
);

-- fact_player_game_stats: 317 columns
CREATE TABLE IF NOT EXISTS fact_player_game_stats (
    player_game_key VARCHAR(50) PRIMARY KEY,
    game_id INTEGER,
    player_id VARCHAR(20),
    player_name VARCHAR(200),
    goals INTEGER,
    assists INTEGER,
    points INTEGER,
    shots INTEGER,
    sog INTEGER,
    shots_blocked INTEGER,
    shots_missed INTEGER,
    shooting_pct DECIMAL(10,4),
    pass_attempts INTEGER,
    pass_completed INTEGER,
    pass_pct DECIMAL(10,4),
    fo_wins INTEGER,
    fo_losses INTEGER,
    fo_total INTEGER,
    fo_pct DECIMAL(10,4),
    zone_entries INTEGER,
    zone_exits INTEGER,
    giveaways INTEGER,
    takeaways INTEGER,
    toi_seconds INTEGER,
    toi_minutes INTEGER,
    playing_toi_seconds INTEGER,
    playing_toi_minutes INTEGER,
    stoppage_seconds INTEGER,
    shift_count INTEGER,
    logical_shifts INTEGER,
    avg_shift INTEGER,
    avg_playing_shift INTEGER,
    goals_per_60 DECIMAL(12,4),
    assists_per_60 DECIMAL(12,4),
    points_per_60 DECIMAL(12,4),
    shots_per_60 DECIMAL(12,4),
    goals_per_60_playing DECIMAL(12,4),
    assists_per_60_playing DECIMAL(12,4),
    points_per_60_playing DECIMAL(12,4),
    shots_per_60_playing DECIMAL(12,4),
    blocks INTEGER,
    hits INTEGER,
    puck_battles INTEGER,
    puck_battle_wins INTEGER,
    retrievals INTEGER,
    corsi_for INTEGER,
    corsi_against INTEGER,
    fenwick_for INTEGER,
    fenwick_against INTEGER,
    cf_pct DECIMAL(10,4),
    ff_pct DECIMAL(10,4),
    opp_avg_rating INTEGER,
    skill_diff DECIMAL(15,6),
    plus_ev INTEGER,
    minus_ev DECIMAL(15,6),
    plus_minus_ev DECIMAL(15,6),
    plus_total INTEGER,
    minus_total DECIMAL(15,6),
    plus_minus_total DECIMAL(15,6),
    plus_en_adj INTEGER,
    minus_en_adj DECIMAL(15,6),
    plus_minus_en_adj DECIMAL(15,6),
    player_rating INTEGER,
    home_team_id VARCHAR(20),
    away_team_id VARCHAR(20),
    chips INTEGER,
    breakout_rush_attempts INTEGER,
    zone_keepins INTEGER,
    dekes_successful INTEGER,
    breakout_clear_attempts INTEGER,
    beat_middle INTEGER,
    beat_wide INTEGER,
    zone_exit_denials INTEGER,
    breakouts INTEGER,
    passes_missed_target INTEGER,
    drives_corner INTEGER,
    tip_attempts INTEGER,
    cutbacks INTEGER,
    drives_middle INTEGER,
    pass_for_tip INTEGER,
    poke_checks INTEGER,
    stick_checks INTEGER,
    got_beat_middle INTEGER,
    crash_net INTEGER,
    cycle_plays INTEGER,
    ceded_exits INTEGER,
    reverse_passes INTEGER,
    deke_attempts INTEGER,
    breakout_pass_attempts INTEGER,
    backchecks INTEGER,
    separate_from_puck INTEGER,
    rebound_recoveries INTEGER,
    block_attempts INTEGER,
    drives_net INTEGER,
    ceded_entries INTEGER,
    puck_retrievals INTEGER,
    passes_intercepted INTEGER,
    stopped_deke INTEGER,
    dump_and_chase INTEGER,
    def_pass_deflected INTEGER,
    loose_puck_wins INTEGER,
    drives_wide INTEGER,
    puck_recoveries INTEGER,
    def_pass_intercepted INTEGER,
    screens INTEGER,
    front_of_net INTEGER,
    secondary_assists INTEGER,
    in_lane INTEGER,
    quick_ups INTEGER,
    second_touches INTEGER,
    delays INTEGER,
    loose_puck_losses INTEGER,
    turnover_recoveries INTEGER,
    dump_recoveries INTEGER,
    pressures INTEGER,
    contains INTEGER,
    forechecks INTEGER,
    give_and_go INTEGER,
    passes_deflected INTEGER,
    lost_puck INTEGER,
    force_wide INTEGER,
    man_on_man INTEGER,
    dekes_beat_defender INTEGER,
    box_outs INTEGER,
    primary_assists INTEGER,
    got_beat_wide INTEGER,
    zone_entry_denials INTEGER,
    surf_plays INTEGER,
    blocked_shots_play INTEGER,
    drives_middle_success INTEGER,
    drives_wide_success INTEGER,
    drives_corner_success INTEGER,
    zone_entry_denials_success INTEGER,
    breakouts_success INTEGER,
    zone_entry_carry INTEGER,
    zone_entry_pass INTEGER,
    zone_entry_dump INTEGER,
    zone_exit_carry INTEGER,
    zone_exit_pass INTEGER,
    zone_exit_dump INTEGER,
    zone_exit_clear INTEGER,
    zone_entries_controlled INTEGER,
    zone_entries_uncontrolled INTEGER,
    zone_entry_success_rate DECIMAL(10,4),
    zone_entry_control_pct DECIMAL(10,4),
    zone_exits_controlled INTEGER,
    zone_exits_uncontrolled INTEGER,
    zone_exit_success_rate DECIMAL(10,4),
    zone_exit_control_pct DECIMAL(10,4),
    def_shots_against INTEGER,
    def_goals_against INTEGER,
    def_entries_allowed INTEGER,
    def_exits_denied INTEGER,
    def_times_beat_deke INTEGER,
    def_times_beat_speed INTEGER,
    def_times_beat_total INTEGER,
    def_takeaways INTEGER,
    def_forced_turnovers INTEGER,
    def_zone_clears INTEGER,
    def_blocked_shots INTEGER,
    def_interceptions INTEGER,
    def_stick_checks INTEGER,
    def_poke_checks INTEGER,
    def_body_checks INTEGER,
    def_coverage_assignments INTEGER,
    def_battles_won INTEGER,
    def_battles_lost INTEGER,
    giveaways_bad INTEGER,
    giveaways_neutral INTEGER,
    giveaways_good INTEGER,
    turnover_diff_adjusted INTEGER,
    turnovers_oz INTEGER,
    turnovers_nz INTEGER,
    turnovers_dz INTEGER,
    giveaway_rate_per_60 INTEGER,
    takeaway_rate_per_60 INTEGER,
    goals_rating_adj DECIMAL(12,4),
    assists_rating_adj DECIMAL(12,4),
    points_rating_adj DECIMAL(12,4),
    plus_minus_rating_adj DECIMAL(15,6),
    cf_pct_rating_adj DECIMAL(10,4),
    qoc_rating INTEGER,
    qot_rating INTEGER,
    expected_vs_rating DECIMAL(15,6),
    xg_for INTEGER,
    xg_against INTEGER,
    xg_diff INTEGER,
    xg_pct DECIMAL(10,4),
    goals_above_expected DECIMAL(12,4),
    shots_high_danger INTEGER,
    shots_medium_danger INTEGER,
    shots_low_danger INTEGER,
    scoring_chances INTEGER,
    high_danger_chances INTEGER,
    xg_per_shot INTEGER,
    shot_quality_avg INTEGER,
    offensive_rating INTEGER,
    defensive_rating INTEGER,
    hustle_rating INTEGER,
    playmaking_rating INTEGER,
    shooting_rating INTEGER,
    physical_rating INTEGER,
    impact_score DECIMAL(15,6),
    war_estimate INTEGER,
    avg_shift_too_long INTEGER,
    shift_length_warning INTEGER,
    fatigue_indicator INTEGER,
    sub_equity_score INTEGER,
    toi_vs_team_avg INTEGER,
    period_3_dropoff INTEGER,
    late_game_performance INTEGER,
    on_ice_sh_pct DECIMAL(10,4),
    on_ice_sv_pct DECIMAL(10,4),
    pdo INTEGER,
    pdo_5v5 INTEGER,
    fo_wins_oz INTEGER,
    fo_wins_nz INTEGER,
    fo_wins_dz INTEGER,
    fo_losses_oz INTEGER,
    fo_losses_nz INTEGER,
    fo_losses_dz INTEGER,
    fo_pct_oz DECIMAL(10,4),
    fo_pct_nz DECIMAL(10,4),
    fo_pct_dz DECIMAL(10,4),
    zone_starts_oz_pct DECIMAL(10,4),
    zone_starts_dz_pct DECIMAL(10,4),
    game_score DECIMAL(15,6),
    game_score_per_60 DECIMAL(15,6),
    game_score_rating INTEGER,
    effective_game_rating INTEGER,
    rating_performance_delta DECIMAL(15,6),
    playing_above_rating INTEGER,
    playing_below_rating INTEGER,
    playing_at_rating INTEGER,
    performance_tier VARCHAR(100),
    performance_index INTEGER,
    shots_successful INTEGER,
    shots_unsuccessful INTEGER,
    passes_successful INTEGER,
    passes_unsuccessful INTEGER,
    plays_successful INTEGER,
    plays_unsuccessful INTEGER,
    entries_successful INTEGER,
    entries_unsuccessful INTEGER,
    exits_successful INTEGER,
    exits_unsuccessful INTEGER,
    total_successful_plays INTEGER,
    total_unsuccessful_plays INTEGER,
    overall_success_rate DECIMAL(10,4),
    shot_success_rate DECIMAL(10,4),
    pass_success_rate DECIMAL(10,4),
    play_success_rate DECIMAL(10,4),
    times_pass_target INTEGER,
    passes_received_successful INTEGER,
    passes_received_unsuccessful INTEGER,
    pass_reception_rate DECIMAL(10,4),
    times_target_oz INTEGER,
    times_target_nz INTEGER,
    times_target_dz INTEGER,
    slot_passes_received INTEGER,
    cross_ice_passes_received INTEGER,
    odd_man_rushes INTEGER,
    odd_man_rush_goals INTEGER,
    odd_man_rush_shots INTEGER,
    breakaway_attempts INTEGER,
    breakaway_goals INTEGER,
    2on1_rushes INTEGER,
    3on2_rushes INTEGER,
    2on0_rushes INTEGER,
    rush_entries INTEGER,
    rush_shots INTEGER,
    rush_goals INTEGER,
    counter_attacks INTEGER,
    transition_plays INTEGER,
    times_targeted_by_opp INTEGER,
    times_targeted_shots INTEGER,
    times_targeted_entries INTEGER,
    times_targeted_passes INTEGER,
    times_targeted_as_defender INTEGER,
    defensive_assignments INTEGER,
    times_attacked INTEGER,
    times_attacked_successfully INTEGER,
    times_attacked_unsuccessfully INTEGER,
    defensive_success_rate DECIMAL(10,4),
    times_ep3 INTEGER,
    times_ep4 INTEGER,
    times_ep5 INTEGER,
    times_opp2 INTEGER,
    times_opp3 INTEGER,
    times_opp4 INTEGER,
    total_on_ice_events INTEGER,
    puck_touches_estimated INTEGER,
    involvement_rate DECIMAL(10,4),
    support_plays INTEGER,
    goals_leading INTEGER,
    goals_trailing INTEGER,
    goals_tied INTEGER,
    shots_leading INTEGER,
    shots_trailing INTEGER,
    shots_tied INTEGER,
    first_period_points INTEGER,
    second_period_points INTEGER,
    third_period_points INTEGER,
    first_period_shots INTEGER,
    second_period_shots INTEGER,
    third_period_shots INTEGER,
    clutch_goals INTEGER,
    empty_net_goals_for INTEGER,
    shorthanded_goals INTEGER,
    offensive_contribution INTEGER,
    defensive_contribution INTEGER,
    two_way_rating INTEGER,
    puck_possession_index INTEGER,
    danger_creation_rate DECIMAL(10,4),
    efficiency_score DECIMAL(15,6),
    clutch_factor INTEGER,
    complete_player_score INTEGER
);

-- fact_team_game_stats: 52 columns
CREATE TABLE IF NOT EXISTS fact_team_game_stats (
    team_game_key VARCHAR(50) PRIMARY KEY,
    game_id INTEGER,
    venue VARCHAR(20),
    goals INTEGER,
    shots INTEGER,
    fo_wins INTEGER,
    giveaways INTEGER,
    takeaways INTEGER,
    zone_entries INTEGER,
    zone_exits INTEGER,
    pass_attempts INTEGER,
    pass_completed INTEGER,
    fo_losses INTEGER,
    sog INTEGER,
    fo_total INTEGER,
    fo_pct DECIMAL(10,4),
    pass_pct DECIMAL(10,4),
    shooting_pct DECIMAL(10,4),
    venue_id INTEGER,
    home_team_id VARCHAR(20),
    away_team_id VARCHAR(20),
    team_id VARCHAR(20),
    total_shots INTEGER,
    total_sog INTEGER,
    total_passes INTEGER,
    total_pass_completed INTEGER,
    team_pass_pct DECIMAL(10,4),
    total_zone_entries INTEGER,
    total_zone_exits INTEGER,
    controlled_entries INTEGER,
    controlled_exits INTEGER,
    entry_control_pct DECIMAL(10,4),
    total_giveaways INTEGER,
    total_takeaways INTEGER,
    turnover_diff DECIMAL(15,6),
    total_blocks INTEGER,
    total_hits INTEGER,
    total_fo_wins INTEGER,
    total_fo_losses INTEGER,
    corsi_for INTEGER,
    corsi_against INTEGER,
    cf_pct DECIMAL(10,4),
    xg_for INTEGER,
    xg_against INTEGER,
    xg_diff INTEGER,
    avg_player_rating INTEGER,
    total_dekes INTEGER,
    total_screens INTEGER,
    offensive_rating_avg INTEGER,
    defensive_rating_avg INTEGER,
    hustle_rating_avg INTEGER,
    team_name VARCHAR(200)
);

-- fact_goalie_game_stats: 19 columns
CREATE TABLE IF NOT EXISTS fact_goalie_game_stats (
    goalie_game_key VARCHAR(50) PRIMARY KEY,
    game_id INTEGER,
    player_id VARCHAR(20),
    player_name VARCHAR(200),
    team_name VARCHAR(200),
    saves INTEGER,
    goals_against INTEGER,
    shots_against INTEGER,
    save_pct DECIMAL(10,4),
    toi_seconds INTEGER,
    empty_net_ga BOOLEAN,
    saves_rebound INTEGER,
    saves_freeze INTEGER,
    saves_glove INTEGER,
    saves_blocker INTEGER,
    saves_left_pad INTEGER,
    saves_right_pad INTEGER,
    rebound_control_pct DECIMAL(10,4),
    total_save_events INTEGER
);

-- fact_h2h: 24 columns
CREATE TABLE IF NOT EXISTS fact_h2h (
    game_id INTEGER,
    player_1_id VARCHAR(20),
    player_2_id VARCHAR(20),
    shifts_together INTEGER,
    home_team_id VARCHAR(20),
    away_team_id VARCHAR(20),
    toi_together INTEGER,
    goals_for DECIMAL(12,4),
    goals_against DECIMAL(12,4),
    plus_minus INTEGER,
    corsi_for INTEGER,
    corsi_against INTEGER,
    cf_pct DECIMAL(10,4),
    fenwick_for INTEGER,
    fenwick_against INTEGER,
    ff_pct DECIMAL(10,4),
    xgf INTEGER,
    xga INTEGER,
    xg_diff INTEGER,
    shots_for DECIMAL(12,4),
    shots_against DECIMAL(12,4),
    h2h_key VARCHAR(50) PRIMARY KEY,
    player_1_name VARCHAR(200),
    player_2_name VARCHAR(200)
);

-- fact_wowy: 28 columns
CREATE TABLE IF NOT EXISTS fact_wowy (
    game_id INTEGER,
    player_1_id VARCHAR(20),
    player_2_id VARCHAR(20),
    venue VARCHAR(20),
    shifts_together INTEGER,
    p1_shifts_without_p2 INTEGER,
    p2_shifts_without_p1 DECIMAL(15,6),
    p1_logical_shifts INTEGER,
    p2_logical_shifts INTEGER,
    venue_id INTEGER,
    home_team_id VARCHAR(20),
    away_team_id VARCHAR(20),
    toi_together INTEGER,
    toi_apart INTEGER,
    gf_pct_together DECIMAL(10,4),
    gf_pct_apart DECIMAL(10,4),
    gf_pct_delta DECIMAL(10,4),
    cf_pct_together DECIMAL(10,4),
    cf_pct_apart DECIMAL(10,4),
    cf_pct_delta DECIMAL(10,4),
    xgf_pct_together DECIMAL(10,4),
    xgf_pct_apart DECIMAL(10,4),
    xgf_pct_delta DECIMAL(10,4),
    relative_corsi DECIMAL(15,6),
    relative_fenwick INTEGER,
    wowy_key VARCHAR(50) PRIMARY KEY,
    player_1_name VARCHAR(200),
    player_2_name VARCHAR(200)
);

